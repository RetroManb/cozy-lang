import cozy.std;
import cozy.string;

local count = 100;

for (local i = 1; i <= count; i++)
{
	local out = "";
	
	if (i % 3 == 0) {
		out += "Fizz";
	}
	if (i % 5 == 0) {
		out += "Buzz";
	}
	if (out == "") {
		out = string.ToString(i);
	}
	
	print(out);
}