import cozy.std;

import Color;
import SingletonTest;

print(Color.FromRGB(0.5,0,1) * Color.FromRGB(0.5,0.5,0.5));

local singleton = Singleton.GetInstance();

print(singleton);

singleton.Value += 1;